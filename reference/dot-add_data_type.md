# Builds the file paths for one or several file types

Builds the file paths for one or several file types

## Usage

``` r
.add_data_type(file_dir, type)
```

## Arguments

- file_dir:

  file path without or with file type ending

- type:

  one or several file types

## Value

file paths, one per given type
