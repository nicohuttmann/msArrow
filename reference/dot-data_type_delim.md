# Delimiter used for a delimited file type

Delimiter used for a delimited file type

## Usage

``` r
.data_type_delim(type)
```

## Arguments

- type:

  file type ("tsv", "csv" or "txt")

## Value

single character delimiter (NA for non-delimited types)
