# Splits into a row count and a number of datasets

Splits into a row count and a number of datasets

## Usage

``` r
.preview_n(n)
```

## Arguments

- n:

  one number, or two as c(rows, datasets)

## Value

list with the elements `rows` and `sets`
