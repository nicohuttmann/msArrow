% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preview_data.R
\name{view_data}
\alias{view_data}
\title{Opens the first rows of a file in the data viewer}
\usage{
view_data(
  file,
  n = 10,
  fallback,
  recursive = T,
  credit = 10,
  title,
  silent = F,
  ...
)
}
\arguments{
\item{file}{file name}

\item{n}{number of rows to read}

\item{fallback}{other file name or alternative way to provide the input -
useful if file is an R object and fallback is a hardcoded string}

\item{recursive}{Should data be recursively loaded?}

\item{credit}{how many recursive steps are allowed}

\item{title}{title of the viewer tab (default: the file name)}

\item{silent}{Should the message reporting the row counts be suppressed?}

\item{...}{additional arguments for the opening function}
}
\value{
the previewed tibble (invisibly), or a named list of them for a
\code{.parquetlist}
}
\description{
\code{view_data()} is \code{\link[=preview_data]{preview_data()}} followed by \code{utils::View()}: the first \if{html}{\out{<n>}}
rows are collected without reading the rest of the file and handed to the
viewer. The viewer is only called in an interactive session, and the preview
is returned invisibly either way, so the same call is safe inside a script.
}
\examples{
  data_small <- tibble::tibble(a = 1:100, b = rnorm(100)) \%>\%
    write_data(file = "data_small", 
               dir = tempdir(), 
               type = "parquet")

  view_data(data_small, n = 5)
}
