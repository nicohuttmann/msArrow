% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preview_data.R
\name{preview_data}
\alias{preview_data}
\title{Reads the first rows of a file without loading the rest}
\usage{
preview_data(
  file,
  n = 10,
  fallback,
  recursive = T,
  credit = 10,
  silent = F,
  ...
)
}
\arguments{
\item{file}{file name}

\item{n}{number of rows to read, or \code{c(rows, datasets)} for a
\code{.parquetlist}, where the second number defaults to 1}

\item{fallback}{other file name or alternative way to provide the input -
useful if file is an R object and fallback is a hardcoded string}

\item{recursive}{Should data be recursively loaded?}

\item{credit}{how many recursive steps are allowed}

\item{silent}{Should the message reporting the row counts be suppressed?}

\item{...}{additional arguments for the opening function}
}
\value{
a tibble of the first \if{html}{\out{<n>}} rows carrying a \code{total_rows} attribute,
or a named list of such tibbles for a \code{.parquetlist}
}
\description{
\code{preview_data()} opens \if{html}{\out{<file>}} lazily with \code{\link[=open_data]{open_data()}} and collects only the
first \if{html}{\out{<n>}} rows, so a multi-million-row file can be inspected without reading
it. It takes everything \code{open_data()} takes: .parquet, .tsv, .csv, .txt,
.Rds, a partitioned dataset directory, or data that is already in the
session.
}
\details{
The total number of rows comes from the file metadata, which costs nothing,
and is attached to the result as the attribute \code{total_rows}; unless \if{html}{\out{<silent>}}
is TRUE a line reporting \verb{Showing <n> of <total_rows> rows} is printed. The
result is otherwise a plain tibble.

A \code{.parquetlist} folder holds several datasets rather than a single table.
Only the first is shown by default; give \if{html}{\out{<n>}} a second number to ask for
more, so \code{n = c(10, 5)} takes ten rows of each of the first five datasets.
The message then says how many were left out and what to pass to see them.
}
\examples{
  data_small <- tibble::tibble(a = 1:100, b = rnorm(100)) \%>\%
    write_data(file = "data_small", 
               dir = tempdir(), 
               type = "parquet")

  preview_data(data_small, n = 5)
}
